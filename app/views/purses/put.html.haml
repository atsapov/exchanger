%h1 My purse

= form_for(@purse, :url => up_put_path) do |f|
  = render 'shared/error_messages', :object => f.object
  .field
    = f.label 'Put'
    %br
    = f.text_field :money
  .actions
    = f.submit "Put"

