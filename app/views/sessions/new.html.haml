%h1 Sign in

= form_for(:session, :url => sessions_path) do |f|
  .field
    = f.label :login
    %br
    = f.text_field :login
  .field
    = f.label :password
    %br
    = f.password_field :password
  .actions
    = f.submit "Sign in"

